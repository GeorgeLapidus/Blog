<!--

function validate_comment_form ( )
{
	valid = false;

        if ( document.comment_form.text.value != "" )
        {
                alert ( "Ваш комментарий успешно отправлен. После проверки он появится на сайте." );
                valid = true;
        }

        return valid;
}

//-->